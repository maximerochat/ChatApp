import {StyleSheet} from "react-native";

export const signup_style = StyleSheet.create({
    default_container: {
        flex: 1, alignItems: 'center', justifyContent: "space-between", width: "100%", flexDirection: "column"
    },
    button_color: {
        backgroundColor: "blue"
    },
    innerContainer: {
        alignItems: 'center', justifyContent: "flex-start", width: "100%", flexDirection: "column"
    }
});